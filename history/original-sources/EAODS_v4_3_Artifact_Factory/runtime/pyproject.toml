[project]
name = "eaods-runtime"
version = "4.3.0-alpha"
description = "Artifact Factory implementation for the Enterprise AI Operator Documentation Suite."
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0.0",
]

[project.scripts]
eaods = "eaods.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
