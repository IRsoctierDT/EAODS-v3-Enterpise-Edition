# Contributing to EAODS

## Branches

Use short-lived branches:

- `docs/<topic>`
- `feature/<capability>`
- `fix/<issue>`
- `chore/<maintenance>`

## Pull requests

Pull requests must explain business and architecture impact, identify affected EAODS volumes, pass documentation validation, preserve YAML front matter, and exclude secrets.

## Commit convention

Use Conventional Commits.
